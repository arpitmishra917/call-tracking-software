# Telnyx Integration
Primary production telephony. Implemented in `apps/api/src/telephony/telnyx/`. Uses Call Control (`TELNYX_CALL_CONTROL_APP_ID`). Real E2E is verified for provisioning and basic calls.
